const manifest = {
  "loaders": {
    // "deco-sites/niivu-bank/loaders/supabase/mod.ts": $$$2,
  },
  "name": "supabase",
  "baseUrl": import.meta.url,
};

export type Manifest = typeof manifest;

export default manifest;

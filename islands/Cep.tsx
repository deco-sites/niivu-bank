import Component from "$store/components/solicitation/Cep.tsx";
import type { Props } from "$store/components/solicitation/Cep.tsx";

export default function Island(props: Props) {
  return <Component {...props} />;
}

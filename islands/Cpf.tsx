import Component from "$store/components/solicitation/Cpf.tsx";
import type { Props } from "$store/components/solicitation/Cpf.tsx";

export default function Island(props: Props) {
  return <Component {...props} />;
}

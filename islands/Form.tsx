import Component from "$store/components/solicitation/Form.tsx";
import type { Props } from "$store/components/solicitation/Form.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
